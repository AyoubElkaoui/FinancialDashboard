// Local type definitions — replaced Prisma-generated types so the app
// builds without a database connection.
export type Database = "SERVICES" | "MAINTENANCE" | "INTERNATIONAL" | "KEYSER";
export type Role = "ADMIN" | "VIEWER";
